# 桌面挂件列表与缩放设计

## TODAY 列表

- 挂件最多展示 5 条未完成待办，不提供更高数量配置。
- 待办按工作区原始顺序显示；第 6 条及以后保持隐藏。
- 完成当前可见条目后，该条目从未完成列表移除，下一条自动进入前 5 条。
- 圆圈为独立完成按钮；待办文字双击打开挂件内部编辑表单。

## 窗口缩放与比例

- 右下角显示明确缩放手柄，拖动可调整窗口宽度和高度。
- 用户手动宽度不会被数据刷新或自动高度计算重置。
- 内容变化时仅按需要调整高度，并保留当前宽度。
- 预览字号、内边距和条目间距使用容器相对单位，随窗口宽度在可读范围内缩放。

## 测试

- 5 条上限及完成后补位。
- 圆圈完成与双击编辑的独立行为。
- 宽度保持和尺寸限制。
- 缩放手柄调用原生窗口尺寸命令。
