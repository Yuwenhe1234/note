# 恢复出厂设置设计

## 目标

用单一“恢复出厂设置”操作替换“清除全部任务”和“恢复默认设置”。

## 行为

- 点击恢复出厂设置后显示一次普通确认对话框，无需输入确认文字。
- 确认后清空全部任务与今日待办，并恢复所有设置为默认值。
- 取消后不修改任何数据。
- 保留导出、导入和清除已完成任务。

## 验收

- 数据管理页不显示“清除全部任务”或“恢复默认设置”。
- 确认恢复出厂设置后任务、待办和设置均回到初始状态。
