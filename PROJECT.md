# 智能任务备忘录项目说明

> 本文档用于帮助开发者和 AI 助手快速理解项目。后续功能、架构或 UI 发生变化时，应同步更新本文档。

## 1. 项目定位

本项目是一个采用五层 Agent 架构实现的智能任务管理网页应用。它不只是记录任务，还希望通过意图识别、任务拆解、时长估算、学习辅助、风险预警和记忆管理，帮助用户把模糊目标转化为可以执行和跟踪的任务。

当前交互界面采用 Vite + React + TypeScript 构建，数据与设置主要保存在本机；开发预览需要通过 Vite 服务访问。

## 2. 核心目标

- 让用户通过网页表单或自然语言快速创建和管理任务。
- 自动分析任务类型、优先级、预计时长、子任务和潜在风险。
- 为学习类和技能实操类任务提供学习辅助信息。
- 用统一的 Agent 调度核心连接交互入口、能力插件和数据层。
- 保持各层低耦合，使新增入口、新增插件和新增字段尽量不影响现有模块。
- 在复杂能力之上保持清晰、轻量、易理解的 UI。

## 3. 五层架构

> 迁移说明：本表描述的是原始五层架构及其在 `js/` 目录中的模块。自 2026-09 起，界面已迁移到 Vite + React + TypeScript：交互层、数据层与基础能力层位于 `src/`，服务端能力位于 `server/`（Vite 中间件）；L2/L3 仍保留在 `js/` 中、尚未迁移。当前目录见第 4 节。

| 层级 | 名称 | 核心职责 | 扩展原则 | 当前模块 |
| --- | --- | --- | --- | --- |
| L1 | 交互接入层 | 接收用户输入并渲染输出结果 | 新增网页、客户端或对话入口时，只适配统一接入协议 | `web-adapter.js`、`chat-adapter.js` |
| L2 | Agent 调度核心层 | 意图识别、任务路由、记忆管理和流程编排 | 新能力通过注册插件及意图路由接入，避免修改核心调度流程 | `agent.js`、`intent-router.js`、`memory-manager.js`、`plugin-registry.js` |
| L3 | 能力插件层 | 承载具体业务功能 | 新增功能应新增独立插件，已有插件尽量零改动 | 任务分析、学习辅助、风险建议、任务管理、统计插件 |
| L4 | 数据持久层 | 统一存储任务、记忆和用户偏好 | 遵循统一数据范式；非核心字段写入扩展容器 | 数据范式、任务仓库、用户仓库 |
| L5 | 基础能力层 | 提供通用底层能力 | 通用能力集中复用，插件不重复实现 | 日志、事件总线、工具函数、存储驱动 |

### 3.1 依赖方向

页面加载顺序为 `L5 → L4 → L3 → L2 → L1 → 应用入口`。

正常调用流程为：

```text
用户操作
  ↓
L1 交互接入层
  ↓
L2 Agent 调度核心层
  ↓
L3 能力插件层
  ↓
L4 数据持久层
  ↓
L5 基础能力层
```

上层不应绕过 Agent 直接拼接复杂业务流程。跨模块状态变化优先通过事件总线通知，以减少模块之间的直接依赖。

## 4. 当前目录结构

> 界面已迁移到 Vite + React + TypeScript，以下为当前真实目录。原 `js/` 目录保留 L2/L3 等未迁移模块，不再作为运行入口；`css/` 为旧版样式。以 `README.md` 为准。

```text
note/
├─ index.html                 # React 挂载入口
├─ README.md                  # GitHub 首页说明
├─ PROJECT.md                 # 项目说明与持续维护记录
├─ package.json               # 脚本与依赖
├─ vite.config.ts             # Vite + 中间件插件（认证 / 工作区 / AI）
├─ 启动项目.cmd                # Windows 一键启动脚本
├─ src/                       # 前端源码（React + TypeScript）
│  ├─ main.tsx                # 入口：会话门控，登录后渲染 App
│  ├─ App.tsx                 # 主应用（四大视图、任务流程、语音、小组件）
│  ├─ index.css               # 全局样式 / 设计令牌
│  ├─ L1-ui/
│  │  ├─ components/          # 通用组件
│  │  └─ features/            # auth / ai / settings / tasks
│  ├─ L4-data/                # task-model、仓库、backup-schema
│  └─ L5-services/            # reminder-service（浏览器通知）
├─ server/                    # Vite 中间件（Node.js）
│  ├─ auth-routes.ts          # 账户 / 会话接口
│  ├─ auth-store.ts           # scrypt 密码哈希存储
│  ├─ workspace-routes.ts     # 工作区读写接口
│  ├─ workspace-store.ts      # 工作区持久化 + 版本 / 备份
│  ├─ ai-routes.ts            # AI 分析 / 今日待办 / 连接测试
│  ├─ ai-config.ts            # AI 配置存储
│  ├─ provider-registry.ts    # 服务商预设
│  └─ analysis-schema.ts      # AI 返回结构校验
├─ src-tauri/                 # Tauri 桌面壳
├─ docs/superpowers/          # 设计文档（specs）与实施计划（plans）
├─ ui框架/                    # UI 参考图片
└─ js/                        # 遗留原生 JS（L2/L3 未迁移部分）
```

## 5. 当前 UI 页面与状态

### 5.1 已实现的主要界面

- **任务清单**：展示总任务、进行中、已完成和逾期统计；支持搜索、类型筛选、状态筛选、优先级筛选和排序。
- **今日待办**：集中展示当天与逾期任务，支持任务状态操作，并接入浏览器语音识别入口。
- **新建/编辑任务**：使用模态框录入任务，可调用智能分析流程生成类型、优先级、子任务、时长、学习辅助和风险提醒。
- **任务详情**：展示任务概览、进度、子任务、学习资源和风险信息，并提供编辑、删除和状态修改操作。
- **聊天**：自然语言经由 L2 意图路由和 Agent 编排处理，可查询今日待办、任务列表、统计和架构信息。
- **其他功能**：以卡片形式进入聊天、AI 陪伴、今日消息和 Agent 架构页面。
- **设置**：用于用户偏好与数据相关操作。

### 5.2 骨架或演示性质的界面

- **AI 陪伴**：已有头像、状态、声音、动作和启停控件的界面骨架，真实智能陪伴能力仍需后续接入。
- **今日消息**：已有链接输入和 Feed 展示方向，目前属于轻量原型。
- **Agent 架构状态**：用于展示五层架构及已注册插件，主要承担可视化说明和运行状态展示。
- **聊天能力**：目前以规则意图识别和本地插件响应为主，尚未等同于真实大模型对话。

## 6. UI 视觉规范

当前视觉方向为 Apple / Linear 风格，核心原则是“复杂智能，简单界面”。

### 6.1 视觉特征

- 使用白色、浅灰背景和蓝色主强调色。
- 顶部导航采用 sticky 定位与毛玻璃效果。
- 卡片使用浅边框、较大圆角和克制的阴影。
- 按钮以圆角胶囊形为主，重要操作使用蓝色主按钮。
- 状态色遵循：绿色表示成功，橙色表示提醒，红色表示危险或逾期。
- 页面切换和卡片悬停使用短促、轻量的动效。
- 桌面端内容最大宽度约为 `1120px`，并提供移动端导航适配。

### 6.2 明确避免

- 紫蓝渐变、霓虹光效、粒子背景和赛博朋克风格。
- 夸张的 3D、机器人形象和大面积发光效果。
- 高密度 Dashboard 堆叠，以及没有信息价值的装饰。
- 为实现视觉效果而破坏可读性、层级或操作效率。

### 6.3 后续视觉开发原则

- 优先统一设计令牌，再调整具体组件，避免散落硬编码颜色和尺寸。
- 新页面复用现有按钮、卡片、标签、模态框和表单语言。
- 状态变化必须同时有文字或图形提示，不只依赖颜色。
- 动效用于解释状态变化，不应拖慢操作。
- 新增界面需要同时检查桌面端和移动端布局。

## 7. 交互逻辑

### 7.1 页面导航

- 顶部导航负责切换任务清单、今日待办、其他功能和设置。
- “其他功能”中的卡片进入聊天、AI 陪伴、今日消息和 Agent 页面。
- 点击品牌标识返回任务清单。
- 移动端通过菜单按钮展开或收起导航。

### 7.2 任务流程

```text
新建任务
  → 填写基本信息
  → 可选“智能分析”
  → Agent 编排多个能力插件
  → 用户检查或调整结果
  → 保存到任务仓库
  → 事件总线通知相关界面刷新
```

- 点击任务卡片进入详情。
- 勾选任务可在完成和未完成状态之间切换。
- 子任务完成情况会影响任务进度。
- 删除前显示确认对话框。
- 保存、更新、删除和识别结果通过 Toast 给出反馈。
- 模态框支持 `Esc` 关闭，以及 `Ctrl/Command + Enter` 保存。

### 7.3 智能分析流程

当前完整分析由 Agent 统一编排：

1. 任务分析插件识别任务类型、拆解子任务并估算时长。
2. 学习类或实操类任务调用学习辅助插件。
3. 风险建议插件生成风险与应对方案。
4. 记忆管理器根据历史偏好调整时长系数。
5. 结果合并为符合统一数据范式的任务草稿。

### 7.4 对话流程

```text
用户文本
  → ChatAdapter
  → IntentRouter
  → Agent.dispatch()
  → 对应能力插件
  → 结构化结果与回复
  → 对话界面渲染
```

当前支持的典型意图包括：新建任务、分析任务、今日待办、查看任务、统计概览、架构和帮助。

## 8. 数据与扩展约定

### 8.1 数据原则

- 所有任务数据应遵循 `schema.js` 定义的统一范式。
- 稳定、通用的核心字段才进入主结构。
- 试验性、插件专属或未来能力字段写入 `extended_fields`，避免频繁改动核心表结构。
- 数据读写通过仓库和存储驱动完成，UI 不直接操作浏览器存储。

任务步骤使用 `{ id, title, hours, completed }` 结构。`hours` 最小为 `0.25`，并以 `0.25` 小时为刻度；任务总时长由所有步骤时长相加得出，完成进度由已完成步骤数量计算。旧备份中的 `durationMinutes`、`steps[].minutes` 和数字步骤数量会在导入时自动迁移。

任务卡片上的步骤是可勾选的气泡，不直接提供文字或时长输入。右侧箭头打开任务编辑窗口，用户可在那里修改任务信息、步骤名称和小时数，并增删步骤。

任务说明在独立弹窗中编辑，确认后才写回草稿。步骤时长平时以文字展示，双击后可输入任意正数并按 `0.25` 小时规范化；步骤左侧把手支持桌面端拖动排序，编号随排序结果自动更新。

任务数据使用独立的 `goal` 字段保存完成目标。说明与目标在同一弹窗中一起编辑；AI 分析自动生成可验证目标。任务清单将说明与目标分别限制为两行展示，搜索同时匹配标题、说明和目标，旧备份缺少目标时迁移为空字符串。

### 8.2 新增插件

新增具体能力时，优先执行以下步骤：

1. 在 `L3-plugins` 中创建独立插件并继承或遵循基础插件接口。
2. 声明插件能够处理的标准意图。
3. 在 Agent 初始化阶段注册插件。
4. 如涉及自然语言入口，在意图路由器中增加规则。
5. 如涉及 UI，仅在 L1 增加输入和结果展示，不把业务逻辑写入界面层。

### 8.3 新增交互入口

网页、客户端、语音或其他对话入口都应通过 L1 适配器接入，并调用 L2 Agent 的统一接口。新入口不得直接依赖某个具体插件。

### 8.4 新增基础能力

大模型调用、联网搜索、文件解析或其他可复用底层能力，应加入 L5 并提供稳定接口，供多个插件调用。不要在不同插件中重复实现相同基础能力。

## 9. 当前技术特点与限制

### 已具备

- React + TypeScript + Vite 构建的界面，四大视图与任务流程完整可用。
- AI 任务分析（OpenAI 兼容接口），服务端严格校验返回结构并自动重试。
- 本地多账户登录（scrypt 密码哈希、HttpOnly 会话）与每用户工作区隔离。
- 工作区持久化（乐观锁 revision + 自动备份）。
- 任务步骤 0.25 小时刻度、拖动排序、双击编辑时长。
- 今日待办：语音识别、AI 生成、浏览器通知提醒。
- 自动化测试（Vitest + Testing Library）、类型检查与生产构建。
- 桌面端与移动端响应式布局，以及 Tauri 桌面壳。

### 当前限制

- 暂无独立后端服务与多设备同步，数据仅存本机（本地多账户已支持，AI 配置仍为全局共享）。
- 数据主要保存在本机，清理浏览器数据或删除 `.local/` 目录会导致数据丢失。
- 意图识别主要基于规则，不具备开放式语义理解能力。
- 学习辅助、风险分析和时长估算目前属于本地规则能力。
- 搜索、文件解析和真实大模型对话尚未作为可复用 L5 服务接入。
- L2（Agent 编排）与 L3（能力插件）仍停留在遗留 `js/` 目录，尚未迁移到 TypeScript。
- `App.tsx` 文件较大；后续页面继续增加时，应按页面或组件拆分渲染与事件逻辑。

## 10. 后续 UI 开发方向

后续 UI 工作分为两个相互配合的部分：

### 10.1 视觉效果

- 统一页面标题、卡片、表单、空状态和反馈组件的视觉节奏。
- 对照 `ui框架` 中的参考图检查页面还原度。
- 完善移动端导航、筛选栏、模态框和详情页适配。
- 补充加载中、分析中、成功、失败、禁用和无数据状态。
- 保持克制、清晰、低干扰的动效和视觉层级。

### 10.2 交互逻辑

- 明确每个页面的主操作、次操作和返回路径。
- 完善表单校验、错误反馈、保存状态和误操作保护。
- 让智能分析结果可预览、可修改、可重新生成。
- 让聊天入口能将识别出的任务草稿直接带入新建任务流程。
- 统一键盘操作、焦点管理和无障碍语义。
- 将大型 L1 适配器逐步拆分为独立视图或组件模块。

## 11. 开发约束

- 修改前先判断功能属于哪一层，不把跨层职责混在同一文件。
- 优先复用现有组件样式和基础能力。
- 新增插件应保持单一职责，并通过标准意图暴露能力。
- 不为未确认的未来需求预先加入复杂抽象。
- UI 改动必须同时验证视觉表现与实际交互。
- 修改架构、页面、数据字段或核心流程后，同步更新本文档。

## 12. 文档维护记录

| 日期 | 变更内容 |
| --- | --- |
| 2026-08-26 | 根据现有项目代码创建长期维护型说明文档，记录五层架构、当前 UI、视觉规范、交互逻辑和扩展规则。 |
| 2026-09-01 | 任务步骤改为 0.25 小时刻度；任务卡使用完成气泡，编辑集中到箭头打开的窗口；AI 分析采用严格的小时制 JSON 格式。 |
| 2026-09-02 | 任务说明改为独立弹窗；步骤时长改为双击输入；新增拖动排序并移除手动分析占位提示。 |
| 2026-09-02 | 新增独立任务完成目标，支持 AI 生成、手动编辑、任务清单展示和旧备份兼容。 |
| 2026-09-05 | 同步文档至 React + TypeScript 现状：更新目录结构、技术特点与限制，补充本地多账户、工作区持久化、AI 分析、桌面小组件等已实现能力，并新增 README 与 GitHub 配套文档。 |
| 2026-09-05 | 完成桌面组件第一阶段：新增网页配置页与交互式预览，配置随工作区按账户持久化；原生 Tauri 窗口留待第二阶段。 |
| 2026-09-05 | 完成桌面组件第二阶段：新增原生 Tauri 组件窗口（无边框、透明、置底、跳过任务栏），配置页「放入桌面」按钮调用窗口命令；以 `npm run tauri dev`（需 Rust 工具链）运行验证。 |
| 2026-09-05 | 启动第二阶段功能开发：今日消息、AI 陪伴和拓展功能进入网站内二级页面；新增今日消息订阅仓库、手动增量刷新、平台适配器边界、AI 摘要、三天清理，以及插件权限清单骨架。 |

